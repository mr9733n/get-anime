[pytest]
addopts = -ra --strict-markers --strict-config
testpaths = tests
python_files = test_*.py
log_cli = false
log_level = INFO
filterwarnings =
    error
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    stress: slow/manual stress tests
    integration: integration-level tests
